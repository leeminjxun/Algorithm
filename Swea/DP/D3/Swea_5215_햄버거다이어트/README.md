# ✅ SWEA: 5215 햄버거 다이어트 (DP , D3)

# 풀이과정

dp 배열을 T[i] 에 해당하는 메모이제이션을 진행한다.

dp[i] 에서 index 에 해당하는 값을 칼로리로, 입력 값 L 만큼의 배열을 생성한다.

입력값 K[i] 에 대해서 L 부터 K[i] 까지 감소하는 순서대로 원래 있던 값(dp[j]) 과 dp[j - K[i]] 에 Taste Score 를 더합 값을 비교하여,
최댓값을 구한다.